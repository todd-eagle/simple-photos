import React, {useEffect} from 'react'


const ProfilePage = (props) => {
    const {user_id, email} = props.location

    console.log('user_id, email: ',user_id, email)
    // useEffect(() => {
        
    // },[])

    return(
        <>
        </>
    )
}

export default ProfilePage